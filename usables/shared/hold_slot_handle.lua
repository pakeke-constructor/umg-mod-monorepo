

local extends = items.SlotHandle
local HoldSlotHandle = objects.Class("usables:HoldSlotHandle", extends)



function HoldSlotHandle:onItemAdded(item)

end

function HoldSlotHandle:onItemRemoved(item)

end


return HoldSlotHandle

