

local function checkDimension(x)
    -- TODO: impl this function
    
    -- returns `true` if x is a valid dimension,
    -- false otherwise.
    return x
end

typecheck.addType("dimension", checkDimension)