

# worldeditor mod

Allows for editing of worlds.

USAGE:
use the command `/worldeditor` to toggle worldedit on and off.
(Alternatively, use `/worldeditor true` or `/worldeditor false`.)

You must have admin privaledges to do this.



## THINGS TO ADD:
Terrain painter (implement this through the terrain mod)

Entity component editor (provide information about component types and stuff)

Player spawnpoint editor

ctrl-c, ctrl-x and ctrl-v

select multiple entities in rectangular region

Entity spawn tool
Abstract entity spawn tool  (must implement all fields in `.abstract` table.)





